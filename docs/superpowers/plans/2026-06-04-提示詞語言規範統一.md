# 提示詞語言規範統一（抽共用）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development 或 superpowers:executing-plans 逐 task 實作。Steps 用 checkbox（`- [ ]`）追蹤。

**Goal:** 把散落在四處、措辭不一致的「圖片/影片生成提示詞必須用繁體中文」規範，收攏成單一真相源，消除「新增生成入口時自帶一份規範、繞過既有規範」的破口（此破口已在 `helper_generate_prompt` 造成英文提示詞 bug）。

**Architecture:** 新增 `lib/prompt_language.py` 一個模組，匯出 `PROMPT_LANGUAGE_RULE`（給 LLM 的繁中規範字串）與 `helper_prompt_language_clause()`（helper endpoint 用的精簡版）。所有 Python 生成路徑（`prompt_builders_script.py` 兩處、`generate.py` helper endpoint）改引用此常數。markdown 規範（`content-modes.md`、`text_backend_runtime_provider.py` 的 agent 系統 prompt）文字對齊，並在 `content-modes.md` 標註「Python 側真相源為 `lib/prompt_language.py`」交叉提醒。

**Tech Stack:** Python（lib 純函式 + 常數），pytest（`uv run python -m pytest`），ruff。

---

## 背景：四處重複（盤點結論）

| 位置 | 現狀文字 | 類型 |
|------|---------|------|
| `lib/prompt_builders_script.py:142`（`build_narration_prompt`） | 「所有輸出內容必須使用中文。僅 JSON 鍵名和列舉值使用英文。」 | Python system prompt |
| `lib/prompt_builders_script.py:291`（drama build 函式） | 同上（**重複第二份**） | Python system prompt |
| `server/routers/generate.py:980-982`（`helper_generate_prompt`） | 「生成…繁體中文提示詞」「必須只輸出繁體中文 Prompt」（已修，先前為英文） | Python system prompt |
| `server/agent_runtime/text_backend_runtime_provider.py:52` | 「圖片/影片生成 prompt 必須使用繁體中文；JSON 鍵名與模型列舉值可以使用英文，但 scene、action…必須使用繁體中文」 | agent 系統 prompt（執行時注入） |
| `agent_runtime_profile/.claude/references/content-modes.md:30` | 「圖片/影片生成 prompt 使用中文」 | markdown（給 agent 讀） |

問題：措辭不一（「中文」vs「繁體中文」）、JSON 例外說明不一致、且**無單一來源** → 新增入口時容易各寫一份，正是 bug 來源。

## 不在本期範圍

- ❌ 改 `build_*_prompt`（character/clue/scene/prop/location）的組裝邏輯——已有共用（clue→prop、scene→location、共用 `build_style_prompt`），不重複。
- ❌ 把 markdown 規範與 Python 常數做成「同一檔案產生」（跨 markdown/Python 無法共用同一字面值，只對齊文字 + 交叉註記）。
- ❌ 動 assistant 「回覆語言」規範（那是面向使用者回覆，與生成提示詞語言是不同規則）。

---

### Task 1: 新增 lib/prompt_language.py 單一真相源

**Files:**
- Create: `lib/prompt_language.py`
- Test: `tests/test_prompt_language.py`

- [x] **Step 1: 寫失敗測試**

```python
# tests/test_prompt_language.py
from lib.prompt_language import PROMPT_LANGUAGE_RULE, helper_prompt_language_clause


def test_rule_requires_traditional_chinese():
    assert "繁體中文" in PROMPT_LANGUAGE_RULE
    # JSON 鍵名/列舉值例外須明確
    assert "JSON" in PROMPT_LANGUAGE_RULE


def test_helper_clause_is_concise_and_chinese():
    clause = helper_prompt_language_clause()
    assert "繁體中文" in clause
    assert "英文" not in clause.replace("非英文", "")  # 不得殘留「輸出英文」語意
```

- [x] **Step 2: 執行確認失敗**

Run: `uv run python -m pytest tests/test_prompt_language.py -v`
Expected: FAIL — `ModuleNotFoundError: lib.prompt_language`。

- [x] **Step 3: 實作模組**

```python
# lib/prompt_language.py
"""提示詞語言規範的單一真相源。

所有「圖片/影片生成提示詞語言」的規範字串都從這裡取，避免各生成入口
各寫一份、措辭不一或被改成英文（曾發生於 helper_generate_prompt）。

註：markdown 側規範（content-modes.md / agent 系統 prompt）無法共用同一
Python 字面值，請保持文字對齊並在 content-modes.md 標註本檔為真相源。
"""

# 結構化生成（劇本）用的完整規範：要求中文輸出、JSON 鍵名/列舉值例外。
PROMPT_LANGUAGE_RULE = (
    "**重要：所有輸出內容必須使用繁體中文。僅 JSON 鍵名 and 列舉值使用英文。**"
)


def helper_prompt_language_clause() -> str:
    """helper 提示詞生成 endpoint 用的精簡規範句（純文字輸出，非 JSON）。"""
    return "必須只輸出繁體中文 Prompt。"
```

- [x] **Step 4: 執行確認通過**

Run: `uv run python -m pytest tests/test_prompt_language.py -v`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add lib/prompt_language.py tests/test_prompt_language.py
git commit -m "feat(lib): 新增提示詞語言規範單一真相源"
```

---

### Task 2: prompt_builders_script.py 兩處改引用常數

**Files:**
- Modify: `lib/prompt_builders_script.py`（`build_narration_prompt` :142 與 drama build 函式 :291）
- Test: `tests/test_prompt_builders.py`（既有）

- [x] **Step 1: 確認兩處的字面值與 f-string 上下文**

Run: `grep -n "所有輸出內容必須使用中文" lib/prompt_builders_script.py`
讀兩處所在的 f-string / 多行字串，確認可用 `{PROMPT_LANGUAGE_RULE}` 插入而不破壞縮排與相鄰行。

- [x] **Step 2: 寫/更新失敗測試**

在 `tests/test_prompt_builders.py` 新增（或對齊既有）：

```python
def test_narration_prompt_embeds_language_rule():
    from lib.prompt_language import PROMPT_LANGUAGE_RULE
    # 以實際呼叫 build_narration_prompt 的既有 fixture 為準
    prompt = build_narration_prompt(**narration_prompt_kwargs())
    assert PROMPT_LANGUAGE_RULE in prompt
    assert "繁體中文" in prompt
```

> `narration_prompt_kwargs()` 用該測試檔既有建構 build_narration_prompt 參數的方式（grep 現有測試）。drama 路徑加對應一條。

- [x] **Step 3: 執行確認失敗**

Run: `uv run python -m pytest tests/test_prompt_builders.py -k language_rule -v`
Expected: FAIL — 目前字面是「使用中文」非「繁體中文」，且未引用常數。

- [x] **Step 4: 兩處改引用常數**

`lib/prompt_builders_script.py` 頂部 import：

```python
from lib.prompt_language import PROMPT_LANGUAGE_RULE
```

兩處（:142、:291）的字面 `**重要：所有輸出內容必須使用中文。僅 JSON 鍵名和列舉值使用英文。**` 改為 f-string 插值 `{PROMPT_LANGUAGE_RULE}`（保留原行所在的多行字串結構）。

- [x] **Step 5: 執行確認通過**

Run: `uv run python -m pytest tests/test_prompt_builders.py -v`
Expected: PASS

- [x] **Step 6: Commit**

```bash
git add lib/prompt_builders_script.py tests/test_prompt_builders.py
git commit -m "refactor(lib): 劇本生成提示詞語言規範改引用單一真相源"
```

---

### Task 3: generate.py helper endpoint 改引用 clause

**Files:**
- Modify: `server/routers/generate.py:977-986`（`helper_generate_prompt` 的 system_prompt）
- Test: `tests/test_generation_tasks_service.py` 或新增 `tests/test_helper_generate_prompt.py`

- [x] **Step 1: 確認既有測試覆蓋**

Run: `grep -rn "helper_generate_prompt\|helper/generate-prompt\|OptimizePromptRequest" tests/`
若已有測試則對齊；無則新增最小測試檔。

- [x] **Step 2: 寫失敗測試 — system prompt 含繁中 clause**

由於 system_prompt 是函式內區域變數，採「呼叫 endpoint 並斷言不輸出英文規範」較脆。改為**抽出 system prompt 組裝為可測純函式**再斷言：

新增純函式於 `generate.py`（或就近 helper 模組）：

```python
def _build_helper_system_prompt(is_video: bool) -> str:
    from lib.prompt_language import helper_prompt_language_clause
    kind = "Text-to-Video" if is_video else "Text-to-Image"
    focus = (
        "4. 著重於描述主體動作、相機運動（如 推近、向右橫搖）、光影變化與動態效果。"
        if is_video
        else "4. 根據提供的專案視覺風格進行渲染，描述畫面構圖、光影與物件細節。"
    )
    return (
        "你是一個專業的 AI 繪圖與影片動作提示詞生成助手。\n"
        f"你的任務是根據使用者提供的基本描述、專案故事背景與整體風格，生成一段精緻、細節豐富、適合 {kind} 模型的繁體中文提示詞（Prompt）。\n"
        "【規則】\n"
        f"1. {helper_prompt_language_clause()}\n"
        "2. 請直接輸出 Prompt 本身，不要包含 markdown 格式、引號、任何前綴或額外的解釋文字。\n"
        "3. 長度控制在 50 至 120 個字之間。\n"
        f"{focus}"
    )
```

測試：

```python
def test_helper_system_prompt_traditional_chinese():
    from server.routers.generate import _build_helper_system_prompt
    for is_video in (True, False):
        sp = _build_helper_system_prompt(is_video)
        assert "繁體中文" in sp
        assert "英文 Prompt" not in sp
        assert "Zoom in" not in sp  # 相機運動範例已中文化
```

- [x] **Step 3: 執行確認失敗**

Run: `uv run python -m pytest tests/test_helper_generate_prompt.py -v`
Expected: FAIL — `_build_helper_system_prompt` 未定義。

- [x] **Step 4: 實作 — 把內聯 system_prompt 抽成 _build_helper_system_prompt**

把 `generate.py:977-986` 的內聯 `system_prompt = (...)` 替換為呼叫 `_build_helper_system_prompt(is_video)`，並把上面 Step 2 的純函式加到模組層級。

- [x] **Step 5: 執行確認通過**

Run: `uv run python -m pytest tests/test_helper_generate_prompt.py tests/test_generation_tasks_service.py -v`
Expected: PASS

- [x] **Step 6: Commit**

```bash
git add server/routers/generate.py tests/test_helper_generate_prompt.py
git commit -m "refactor(server): helper 提示詞生成 system prompt 抽純函式並引用語言規範"
```

---

### Task 4: markdown 規範文字對齊 + 交叉註記

**Files:**
- Modify: `agent_runtime_profile/.claude/references/content-modes.md:28-30`
- Modify: `server/agent_runtime/text_backend_runtime_provider.py:52`（文字對齊，可選）

- [x] **Step 1: content-modes.md 文字統一 + 標註真相源**

`content-modes.md` 的「Prompt 語言」章節改為：

```markdown
## Prompt 語言

- 圖片/影片生成 prompt 使用**繁體中文**；JSON 鍵名與模型列舉值可用英文。
- （Python 側真相源：`lib/prompt_language.py` 的 `PROMPT_LANGUAGE_RULE`。新增生成入口時請引用該常數，勿自寫一份。）
```

- [x] **Step 2: text_backend_runtime_provider.py 文字確認**

`:52` 已是「必須使用繁體中文」，措辭已正確，僅確認與 content-modes 一致即可（不強制改）。

- [x] **Step 3: 驗證無殘留「使用英文」生成規範**

Run: `grep -rniE "只輸出英文|必須.*輸出.*英文|英文提示詞|生成.*英文.*prompt" server/ lib/ agent_runtime_profile/.claude/ | grep -vi "test"`
Expected: 無輸出（僅 JSON 鍵名/列舉的「可用英文」允許保留）。

- [x] **Step 4: Commit**

```bash
git add agent_runtime_profile/.claude/references/content-modes.md server/agent_runtime/text_backend_runtime_provider.py
git commit -m "docs(agent): 提示詞語言規範文字對齊並標註 Python 真相源"
```

---

### Task 5: 整體驗證

- [x] **Step 1: 後端測試全綠**

Run: `uv run python -m pytest tests/test_prompt_language.py tests/test_prompt_builders_script.py tests/test_helper_generate_prompt.py -v`
Expected: PASS

- [x] **Step 2: lint**

Run: `uv run ruff check lib/prompt_language.py lib/prompt_builders_script.py server/routers/generate.py && uv run ruff format --check lib/prompt_language.py`
Expected: PASS

- [x] **Step 3: 全域重複掃描（確認真相源唯一）**

Run: `grep -rn "所有輸出內容必須使用繁體中文" lib/ server/ | grep -v "lib/prompt_language.py"`
Expected: 無輸出（規範字面只存在於 `prompt_language.py`，其餘皆引用）。

---

## Self-Review 結果

- **Spec coverage**：四處重複 → Task 1（建真相源）、Task 2（script 兩處）、Task 3（helper endpoint）、Task 4（markdown + 交叉註記）全覆蓋。
- **型別一致性**：`PROMPT_LANGUAGE_RULE`（常數）與 `helper_prompt_language_clause()`（函式）兩個名稱跨 Task 1/2/3 一致；`_build_helper_system_prompt(is_video: bool)` 簽名在 Task 3 內自洽。
- **Placeholder 掃描**：無 TBD；測試 fixture 處標明「以既有測試建構方式為準（grep）」並給退化路徑。
- **跨層限制已說明**：markdown 無法共用 Python 字面值，採文字對齊 + 交叉註記，已在 Architecture 與不在範圍說明。
