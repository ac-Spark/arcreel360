# 拆段時自動按集數均分原文 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 拆段（及劇集規範化）某集時：使用者有指定原文來源就用指定的；沒指定，就自動把 `source/` 整本原文按 `project.json` 的集數平均切分，取該集對應的那一段——使用者不需操作「分集」步驟。

**Architecture:** 在 `lib/episode_splitter.py` 新增純函式 `split_into_n_episodes(text, total_episodes)`，用既有的 `count_chars` / `find_char_offset` / `find_natural_breakpoints` 把整本原文按字數均分為 N 段、切點落最近句末標點。在兩支預處理腳本的 `resolve_novel_text()` 內，當某集缺專屬原文且使用者未指定 `source` 時，呼叫此函式取該集片段。「整本原文」= `source/` 內所有文字檔（排除 `_remaining.txt`）按檔名排序串接。HTTP 端與前端新增「指定來源」的可選參數，讓使用者能覆寫自動均分。

**Tech Stack:** Python 3.13、pytest（`asyncio_mode=auto`）、ruff；前端 React 19 + TypeScript。`split_into_n_episodes` 為純函式（無 I/O）。

---

## 背景與設計決策

### 使用者意圖（本計畫的依據）

- 「拆段時自動從整本按集數分」——沒指定來源時的預設行為。
- 「還是要給用戶指定來源——沒指定就這樣（均分），有指定就用指定的」——`source` 為可選覆寫。
- 「整本原文」= `source/` 內所有檔按檔名串接。
- 「共幾集」= `project.json` 現有 `episodes` 數量。
- narration 與 drama 兩模式行為一致。

### 現況

`resolve_novel_text(project_path, episode, source)`（本 session 已修）目前邏輯：有 `source` → 讀該檔；`episode_N.txt` 存在 → 讀它；`episode==1` 單章 fallback；否則 `raise SourceNotReadyError`。

`resolve_novel_text` 的 `source` 參數**機制上已支援指定來源**，但：
- 拆段腳本 `--source` CLI 參數已存在。
- `lib/episode_preprocess.run_preprocess()` 呼叫腳本時**寫死不帶 `--source`**（`["--episode", str(episode)]`）。
- HTTP 路由 `preprocess_episode` 與前端拆段按鈕**沒有傳遞 `source` 的能力**。

因此「有指定就用指定」要打通的是 **run_preprocess → 路由 → 前端** 這條傳遞鏈，而非 `resolve_novel_text` 本身。

### 設計邊界（明確不做）

- **不改既有分集切分流程**：`commit_episode_split` / split 路由 / `split_episode.py` / `_remaining.txt` 機制一律不動。它們仍可用（進階手動精切），其產物 `episode_N.txt` 優先序高於均分。
- **不新增 `project.json` 欄位**。
- **均分不寫檔**：拆段當下即時計算，不落地 `episode_N.txt`（落地會與動態集數衝突）。

### 已知取捨（使用者已確認接受）

- **動態分母**：集數來源為 `project.json` 現有 `episodes` 數量。新增第 N+1 集後分母改變，已拆過舊集的「均分起迄」會位移、內容隨之改變。使用者已選此行為。因拆段本就是「重新拆段」可重跑，影響可控。
- **集的順序**：以 `episode` 數字升序決定「第幾段」。`resolve_novel_text` 內以「該集在升序集列表中的 1-based 索引」對應段落（索引而非裸 `episode` 值，故集號不連續也正確）。

### 與已修 bug 的關係

本 session 已完成「拆段缺原文 → HTTP 400 + 清楚訊息」修復（`SourceNotReadyError`、退出碼 3、路由 400、drama 對齊、前端文案去術語）。本計畫建立在其上。修畢後 `SourceNotReadyError` 僅在「`source/` 完全沒有原文檔或內容為空、且未指定 `source`」時才拋。

---

## File Structure

| 檔案 | 角色 | 本計畫變更 |
|---|---|---|
| `lib/episode_splitter.py` | 分集切分純函式庫 | 新增 `split_into_n_episodes()` |
| `tests/test_episode_splitter.py` | 單元測試 | 加 `split_into_n_episodes` 測試 |
| `agent_runtime_profile/.claude/skills/generate-script/scripts/split_narration_segments.py` | 說書拆段腳本 | `resolve_novel_text` 改用均分 |
| `agent_runtime_profile/.claude/skills/generate-script/scripts/normalize_drama_script.py` | 劇集規範化腳本 | 同上（兩支一致） |
| `tests/scripts/test_split_narration_segments.py` | 說書腳本測試 | 改寫缺原文測試為均分行為 |
| `tests/scripts/test_normalize_drama_script.py` | 劇集腳本測試 | 同上 |
| `lib/episode_preprocess.py` | 預處理編排 | `run_preprocess` 加可選 `source` 參數並傳給腳本 |
| `server/routers/projects.py` | HTTP 路由 | `preprocess_episode` 加可選 `source` body 欄位 |
| `server/agent_runtime/skill_function_declarations.py` | agent 工具 | `_handle_preprocess_episode` 加可選 `source` |
| `frontend/src/api/episodes.ts` | 前端 API client | `preprocessEpisode` 加可選 `source` 參數 |
| `frontend/src/components/canvas/timeline/EpisodeActionsBar.tsx` | 拆段按鈕 | 加「指定來源」可選入口 |
| `tests/test_episode_split_skill.py` | 整合測試 | 加均分 + 指定來源整合測試 |

兩支腳本的 `resolve_novel_text` 必須保持**邏輯一致**（差別僅 `SourceNotReadyError` 訊息動詞：narration「拆段」、drama「規範化」）。

---

## Task 1：`split_into_n_episodes` 純函式

**Files:**
- Modify: `lib/episode_splitter.py`（檔末新增）
- Test: `tests/test_episode_splitter.py`

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_episode_splitter.py` 末尾新增：

```python
def test_split_into_n_episodes_single():
    """N=1：整本即唯一一集。"""
    from lib.episode_splitter import split_into_n_episodes

    text = "第一句。第二句。第三句。"
    parts = split_into_n_episodes(text, 1)
    assert parts == [text]


def test_split_into_n_episodes_two_parts_concatenate_to_original():
    """N=2：兩段串接後等於原文（不丟字、不重複）。"""
    from lib.episode_splitter import split_into_n_episodes

    text = "甲" * 50 + "。" + "乙" * 50 + "。"
    parts = split_into_n_episodes(text, 2)
    assert len(parts) == 2
    assert "".join(parts) == text
    assert min(len(p) for p in parts) >= len(text) // 4


def test_split_into_n_episodes_cuts_on_sentence_end():
    """切點應落在句末標點之後，不切斷句子。"""
    from lib.episode_splitter import split_into_n_episodes

    text = "甲甲甲甲甲。乙乙乙乙乙。丙丙丙丙丙。"
    parts = split_into_n_episodes(text, 3)
    assert len(parts) == 3
    assert "".join(parts) == text
    for part in parts[:-1]:
        assert part.rstrip()[-1] in "。！？…"


def test_split_into_n_episodes_three_parts():
    """N=3：產生 3 段，串接還原。"""
    from lib.episode_splitter import split_into_n_episodes

    text = "".join(f"這是第{i}句話。" for i in range(30))
    parts = split_into_n_episodes(text, 3)
    assert len(parts) == 3
    assert "".join(parts) == text


def test_split_into_n_episodes_invalid_n_raises():
    """N<1 → ValueError。"""
    import pytest

    from lib.episode_splitter import split_into_n_episodes

    with pytest.raises(ValueError):
        split_into_n_episodes("一些文字。", 0)


def test_split_into_n_episodes_more_episodes_than_content():
    """N 大於可切分句子數：仍回傳 N 段，允許部分段為空字串，串接仍還原。"""
    from lib.episode_splitter import split_into_n_episodes

    text = "只有一句。"
    parts = split_into_n_episodes(text, 5)
    assert len(parts) == 5
    assert "".join(parts) == text
```

- [ ] **Step 2: 跑測試，確認失敗**

Run: `uv run python -m pytest tests/test_episode_splitter.py -q -k split_into_n_episodes`
Expected: 全部 FAIL（`ImportError: cannot import name 'split_into_n_episodes'`）。

- [ ] **Step 3: 實作 `split_into_n_episodes`**

在 `lib/episode_splitter.py` 檔末加入：

```python
def split_into_n_episodes(text: str, total_episodes: int) -> list[str]:
    """把整本原文按有效字數均分為 total_episodes 段，切點落最近句末標點。

    供「拆段時自動按集數均分原文」使用：不需錨點、不需使用者指定切點。
    保證 "".join(回傳值) == text（不丟字、不重複）。

    Args:
        text: 整本小說原文。
        total_episodes: 目標段數（= project.json 的集數），須 >= 1。

    Returns:
        長度為 total_episodes 的字串列表，依序為第 1..N 段。
        當內容過短不足以切出 N 段時，末尾段可能為空字串。

    Raises:
        ValueError: total_episodes < 1。
    """
    if total_episodes < 1:
        raise ValueError(f"集數必須 >= 1，得到 {total_episodes}")
    if total_episodes == 1:
        return [text]

    total = count_chars(text)
    cut_offsets: list[int] = []
    for k in range(1, total_episodes):
        target_chars = round(total * k / total_episodes)
        raw_offset = find_char_offset(text, target_chars)
        breakpoints = find_natural_breakpoints(text, raw_offset, window=200)
        sentence_bps = [bp for bp in breakpoints if bp["type"] == "sentence"]
        chosen = sentence_bps or breakpoints
        offset = chosen[0]["offset"] if chosen else raw_offset
        cut_offsets.append(offset)

    # 切點需單調遞增且落在 [0, len(text)]，避免重疊或越界。
    cut_offsets = sorted(min(max(o, 0), len(text)) for o in cut_offsets)

    parts: list[str] = []
    prev = 0
    for offset in cut_offsets:
        parts.append(text[prev:offset])
        prev = offset
    parts.append(text[prev:])
    return parts
```

說明：`count_chars`、`find_char_offset`、`find_natural_breakpoints` 皆為本檔既有函式。`find_natural_breakpoints` 回傳項含 `offset` 與 `type`（`"sentence"`／`"paragraph"`）。

- [ ] **Step 4: 跑測試，確認全綠**

Run: `uv run python -m pytest tests/test_episode_splitter.py -q`
Expected: 全部 PASS。

- [ ] **Step 5: lint**

Run: `uv run ruff check lib/episode_splitter.py tests/test_episode_splitter.py`
Expected: `All checks passed!`

- [ ] **Step 6: Commit**

```bash
git add lib/episode_splitter.py tests/test_episode_splitter.py
git commit -m "feat: 新增 split_into_n_episodes 按集數均分原文純函式"
```

---

## Task 2：narration 腳本 — `resolve_novel_text` 改用均分

**Files:**
- Modify: `agent_runtime_profile/.claude/skills/generate-script/scripts/split_narration_segments.py`
- Test: `tests/scripts/test_split_narration_segments.py`

- [ ] **Step 1: 改寫測試**

在 `tests/scripts/test_split_narration_segments.py`：

**刪除**舊測試（斷言舊「報錯／單章 fallback」行為，已被均分取代）：
`test_resolve_novel_text_episode2_without_split_raises`、`test_resolve_novel_text_episode1_single_file_fallback`、`test_resolve_novel_text_episode1_single_file_ignores_remaining`、`test_resolve_novel_text_episode1_multi_file_raises`、`test_resolve_novel_text_episode1_empty_source_raises`。

**保留**：`test_resolve_novel_text_normal`、`test_resolve_novel_text_explicit_source`、`test_source_not_ready_error_is_filenotfound_subclass`。

**新增**：

```python
import json


def _write_project(project_path, episodes):
    """寫一份最小 project.json，episodes 為 [{"episode": N}, ...]。"""
    (project_path / "project.json").write_text(
        json.dumps({"episodes": episodes}, ensure_ascii=False),
        encoding="utf-8",
    )


def test_resolve_novel_text_episode2_auto_split_from_whole(tmp_path):
    """缺 episode_2.txt 且未指定 source：整本按 2 集均分，第 2 集取後半。"""
    source_dir = tmp_path / "source"
    source_dir.mkdir(parents=True, exist_ok=True)
    (source_dir / "novel.md").write_text("甲" * 40 + "。" + "乙" * 40 + "。", encoding="utf-8")
    _write_project(tmp_path, [{"episode": 1}, {"episode": 2}])

    ep1 = split_narration_segments.resolve_novel_text(tmp_path, episode=1, source=None)
    ep2 = split_narration_segments.resolve_novel_text(tmp_path, episode=2, source=None)

    assert ep1 + ep2 == "甲" * 40 + "。" + "乙" * 40 + "。"
    assert ep1 and ep2


def test_resolve_novel_text_episode1_single_episode_uses_whole(tmp_path):
    """只有 1 集：整本即第 1 集。"""
    source_dir = tmp_path / "source"
    source_dir.mkdir(parents=True, exist_ok=True)
    (source_dir / "novel.md").write_text("整本原文內容。", encoding="utf-8")
    _write_project(tmp_path, [{"episode": 1}])

    result = split_narration_segments.resolve_novel_text(tmp_path, episode=1, source=None)
    assert result == "整本原文內容。"


def test_resolve_novel_text_multi_source_files_concatenated(tmp_path):
    """source/ 多檔：按檔名排序串接後再均分。"""
    source_dir = tmp_path / "source"
    source_dir.mkdir(parents=True, exist_ok=True)
    (source_dir / "01_chapter.md").write_text("第一章內容。", encoding="utf-8")
    (source_dir / "02_chapter.md").write_text("第二章內容。", encoding="utf-8")
    _write_project(tmp_path, [{"episode": 1}])

    result = split_narration_segments.resolve_novel_text(tmp_path, episode=1, source=None)
    assert result == "第一章內容。\n\n第二章內容。"


def test_resolve_novel_text_excludes_remaining_txt(tmp_path):
    """串接整本時排除 _remaining.txt（分集切分的產物）。"""
    source_dir = tmp_path / "source"
    source_dir.mkdir(parents=True, exist_ok=True)
    (source_dir / "novel.md").write_text("正文內容。", encoding="utf-8")
    (source_dir / "_remaining.txt").write_text("不該被串進來", encoding="utf-8")
    _write_project(tmp_path, [{"episode": 1}])

    result = split_narration_segments.resolve_novel_text(tmp_path, episode=1, source=None)
    assert result == "正文內容。"


def test_resolve_novel_text_empty_source_dir_raises(tmp_path):
    """source/ 完全沒有原文檔 → SourceNotReadyError。"""
    (tmp_path / "source").mkdir(parents=True, exist_ok=True)
    _write_project(tmp_path, [{"episode": 1}])

    with pytest.raises(split_narration_segments.SourceNotReadyError):
        split_narration_segments.resolve_novel_text(tmp_path, episode=1, source=None)


def test_resolve_novel_text_prefers_episode_file_over_autosplit(tmp_path):
    """episode_2.txt 存在時，仍優先用它，不走均分。"""
    source_dir = tmp_path / "source"
    source_dir.mkdir(parents=True, exist_ok=True)
    (source_dir / "novel.md").write_text("整本不該被用。", encoding="utf-8")
    (source_dir / "episode_2.txt").write_text("第二集專屬原文。", encoding="utf-8")
    _write_project(tmp_path, [{"episode": 1}, {"episode": 2}])

    result = split_narration_segments.resolve_novel_text(tmp_path, episode=2, source=None)
    assert result == "第二集專屬原文。"


def test_resolve_novel_text_explicit_source_overrides_autosplit(tmp_path):
    """有指定 source 時，用指定的，不走均分（即使整本存在）。"""
    source_dir = tmp_path / "source"
    source_dir.mkdir(parents=True, exist_ok=True)
    (source_dir / "novel.md").write_text("整本不該被用。", encoding="utf-8")
    (source_dir / "我指定的.txt").write_text("使用者指定的原文。", encoding="utf-8")
    _write_project(tmp_path, [{"episode": 1}, {"episode": 2}])

    result = split_narration_segments.resolve_novel_text(
        tmp_path, episode=2, source="source/我指定的.txt"
    )
    assert result == "使用者指定的原文。"
```

- [ ] **Step 2: 跑測試，確認新案例失敗**

Run: `uv run python -m pytest tests/scripts/test_split_narration_segments.py -q`
Expected: auto-split 案例 FAIL（目前缺檔即拋 `SourceNotReadyError`、且不讀 `project.json`）。

- [ ] **Step 3: 改寫 `resolve_novel_text`**

把 `split_narration_segments.py` 中 `resolve_novel_text` 與 `_episode_source_files` 整段替換為：

```python
def _episode_source_files(source_dir: Path) -> list[Path]:
    """列出 source/ 內可視為小說原文的檔案（排除分集切分產生的 _remaining.txt）。"""
    if not source_dir.is_dir():
        return []
    return sorted(
        f
        for f in source_dir.iterdir()
        if f.is_file() and f.suffix.lower() in _SOURCE_SUFFIXES and f.name != "_remaining.txt"
    )


def _read_whole_source(source_dir: Path) -> str:
    """把 source/ 內所有原文檔按檔名排序串接成「整本原文」。"""
    files = _episode_source_files(source_dir)
    return "\n\n".join(f.read_text(encoding="utf-8") for f in files)


def _episode_index_and_total(project_path: Path, episode: int) -> tuple[int, int]:
    """從 project.json 算出 (該集在升序集列表中的 1-based 索引, 總集數)。

    project.json 不存在 / 無 episodes / 該集不在列表 → 退化為 (1, 1)，
    亦即把整本當單一一集，確保仍可拆段。
    """
    import json as _json

    project_json = project_path / "project.json"
    if not project_json.exists():
        return (1, 1)
    try:
        data = _json.loads(project_json.read_text(encoding="utf-8"))
    except (ValueError, OSError):
        return (1, 1)
    episodes = data.get("episodes") or []
    numbers = sorted(
        int(ep["episode"]) for ep in episodes if isinstance(ep, dict) and "episode" in ep
    )
    if not numbers or episode not in numbers:
        return (1, 1)
    return (numbers.index(episode) + 1, len(numbers))


def resolve_novel_text(project_path: Path, episode: int, source: str | None) -> str:
    """決定並讀取某集的小說原文。

    解析順序：
    1. 使用者指定 source → 讀該檔（覆寫自動均分）。
    2. source/episode_{episode}.txt 存在 → 讀它（進階使用者手動精切的產物）。
    3. 否則 → 讀 source/ 整本原文，按 project.json 的集數均分，取本集那一段。

    Raises:
        ValueError: 指定的 source 路徑超出專案目錄。
        FileNotFoundError: 指定的 source 檔不存在。
        SourceNotReadyError: 未指定 source 且 source/ 內無原文檔／整本內容為空。
    """
    if source:
        project_root = project_path.resolve()
        source_path = (project_path / source).resolve()
        if not source_path.is_relative_to(project_root):
            raise ValueError(f"路徑超出專案目錄: {source_path}")
        if not source_path.exists():
            raise FileNotFoundError(f"未找到原始檔: {source_path}")
        return source_path.read_text(encoding="utf-8")

    candidate = project_path / "source" / f"episode_{episode}.txt"
    if candidate.exists():
        return candidate.read_text(encoding="utf-8")

    whole = _read_whole_source(project_path / "source")
    if not whole.strip():
        raise SourceNotReadyError(
            "source/ 內沒有可用的小說原文。請先在資產面板上傳小說原文檔，再執行拆段。"
        )

    from lib.episode_splitter import split_into_n_episodes

    index, total = _episode_index_and_total(project_path, episode)
    parts = split_into_n_episodes(whole, total)
    return parts[index - 1]
```

說明：`_SOURCE_SUFFIXES`、`SourceNotReadyError` 本 session 已於檔內定義，保留。移除舊「episode 1 單章 fallback」與「episode≥2 報錯」分支。

- [ ] **Step 4: 跑測試，確認全綠**

Run: `uv run python -m pytest tests/scripts/test_split_narration_segments.py -q`
Expected: 全部 PASS。

- [ ] **Step 5: lint**

Run: `uv run ruff check agent_runtime_profile/.claude/skills/generate-script/scripts/split_narration_segments.py tests/scripts/test_split_narration_segments.py`
Expected: `All checks passed!`

- [ ] **Step 6: Commit**

```bash
git add agent_runtime_profile/.claude/skills/generate-script/scripts/split_narration_segments.py tests/scripts/test_split_narration_segments.py
git commit -m "feat: 說書拆段缺原文時自動按集數均分整本原文"
```

---

## Task 3：drama 腳本 — `resolve_novel_text` 改用均分

**Files:**
- Modify: `agent_runtime_profile/.claude/skills/generate-script/scripts/normalize_drama_script.py`
- Test: `tests/scripts/test_normalize_drama_script.py`

- [ ] **Step 1: 改寫測試**

對 `tests/scripts/test_normalize_drama_script.py`：
- **刪除**舊測試：`test_resolve_novel_text_episode2_without_split_raises`、`test_resolve_novel_text_episode1_single_file_fallback`、`test_resolve_novel_text_episode1_single_file_ignores_remaining`、`test_resolve_novel_text_episode1_multi_file_raises`、`test_resolve_novel_text_episode1_empty_source_raises`。
- **保留**：`test_resolve_novel_text_normal`、`test_resolve_novel_text_explicit_source`、`test_resolve_novel_text_explicit_source_path_escape`、`test_source_not_ready_error_is_filenotfound_subclass`。
- **新增**：與 Task 2 Step 1 相同的 7 個測試（含 `_write_project` helper、`test_resolve_novel_text_explicit_source_overrides_autosplit`），把模組名 `split_narration_segments` 全部替換為 `normalize_drama_script`。逐字照搬 Task 2 Step 1 新增區塊、僅替換模組名。

- [ ] **Step 2: 跑測試，確認新案例失敗**

Run: `uv run python -m pytest tests/scripts/test_normalize_drama_script.py -q`
Expected: auto-split 案例 FAIL。

- [ ] **Step 3: 改寫 `resolve_novel_text`**

把 `normalize_drama_script.py` 的 `_episode_source_files` + `resolve_novel_text` 替換為**與 Task 2 Step 3 完全相同的程式碼**（`_episode_source_files`、`_read_whole_source`、`_episode_index_and_total`、`resolve_novel_text` 逐字照搬），唯一差別是 `SourceNotReadyError` 訊息：

```python
        raise SourceNotReadyError(
            "source/ 內沒有可用的小說原文。請先在資產面板上傳小說原文檔，再執行規範化。"
        )
```

`_SOURCE_SUFFIXES`、`SourceNotReadyError` 本 session 已於 drama 腳本內定義，保留。

- [ ] **Step 4: 跑測試，確認全綠**

Run: `uv run python -m pytest tests/scripts/test_normalize_drama_script.py -q`
Expected: 全部 PASS。

- [ ] **Step 5: lint**

Run: `uv run ruff check agent_runtime_profile/.claude/skills/generate-script/scripts/normalize_drama_script.py tests/scripts/test_normalize_drama_script.py`
Expected: `All checks passed!`

- [ ] **Step 6: Commit**

```bash
git add agent_runtime_profile/.claude/skills/generate-script/scripts/normalize_drama_script.py tests/scripts/test_normalize_drama_script.py
git commit -m "feat: 劇集規範化缺原文時自動按集數均分整本原文"
```

---

## Task 4：`run_preprocess` 傳遞可選 `source`

**Files:**
- Modify: `lib/episode_preprocess.py`（`run_preprocess` 簽名與 subprocess 命令）
- Test: `tests/test_episode_split_skill.py`

背景：`run_preprocess` 目前呼叫腳本固定 `["--episode", str(episode)]`，無法把使用者指定的來源傳下去。本 Task 加可選 `source` 參數。

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_episode_split_skill.py` 末尾新增：

```python
def test_run_preprocess_passes_source_to_script(tmp_path):
    """run_preprocess 帶 source 時，腳本應讀指定檔（而非自動均分）。"""
    from lib import PROJECT_ROOT
    from lib.episode_preprocess import SourceNotReadyError, run_preprocess
    from lib.project_manager import ProjectManager

    pm = ProjectManager(projects_root=tmp_path / "projects")
    pm.create_project("demo")
    pm.create_project_metadata("demo", title="t", style="anime", content_mode="narration")
    project_path = pm.get_project_path("demo")
    (project_path / "source" / "指定原文.md").write_text("使用者指定的內容。", encoding="utf-8")

    try:
        run_preprocess(
            project_path,
            episode=1,
            content_mode="narration",
            repo_root=PROJECT_ROOT,
            source="source/指定原文.md",
        )
    except SourceNotReadyError:
        raise AssertionError("已指定有效 source，不應拋 SourceNotReadyError")
    except RuntimeError:
        # 原文解析通過；後續真實 backend 呼叫失敗屬預期。
        pass


def test_run_preprocess_invalid_source_fails(tmp_path):
    """run_preprocess 帶不存在的 source → 腳本退出碼非 0 → RuntimeError。"""
    from lib import PROJECT_ROOT
    from lib.episode_preprocess import run_preprocess
    from lib.project_manager import ProjectManager

    pm = ProjectManager(projects_root=tmp_path / "projects")
    pm.create_project("demo")
    pm.create_project_metadata("demo", title="t", style="anime", content_mode="narration")
    project_path = pm.get_project_path("demo")

    with pytest.raises(RuntimeError):
        run_preprocess(
            project_path,
            episode=1,
            content_mode="narration",
            repo_root=PROJECT_ROOT,
            source="source/不存在.md",
        )
```

- [ ] **Step 2: 跑測試，確認失敗**

Run: `uv run python -m pytest tests/test_episode_split_skill.py -q -k "passes_source or invalid_source"`
Expected: FAIL（`run_preprocess` 目前無 `source` 參數，`TypeError: unexpected keyword argument 'source'`）。

- [ ] **Step 3: 加 `source` 參數**

在 `lib/episode_preprocess.py` 的 `run_preprocess`：

簽名改為：
```python
def run_preprocess(
    project_path: Path,
    episode: int,
    *,
    content_mode: str | None = None,
    repo_root: Path | None = None,
    source: str | None = None,
) -> dict:
```

docstring `Args:` 補一行：
```
        source: 可選。指定本集小說原文的相對路徑（須在專案目錄內）。
            省略時腳本自動按集數均分整本原文。
```

subprocess 命令組裝改為：
```python
    cmd = [sys.executable, str(skill_script), "--episode", str(episode)]
    if source:
        cmd += ["--source", source]
    try:
        proc = subprocess.run(
            cmd,
            cwd=str(project_path),
            capture_output=True,
            text=True,
            timeout=_PREPROCESS_TIMEOUT,
        )
```
（即把原本寫死的 list 抽成 `cmd` 變數，條件附加 `--source`。）

- [ ] **Step 4: 跑測試，確認全綠**

Run: `uv run python -m pytest tests/test_episode_split_skill.py -q`
Expected: 全部 PASS。

- [ ] **Step 5: lint**

Run: `uv run ruff check lib/episode_preprocess.py tests/test_episode_split_skill.py`
Expected: `All checks passed!`

- [ ] **Step 6: Commit**

```bash
git add lib/episode_preprocess.py tests/test_episode_split_skill.py
git commit -m "feat: run_preprocess 支援可選指定原文來源"
```

---

## Task 5：HTTP 路由 + agent 工具支援可選 `source`

**Files:**
- Modify: `server/routers/projects.py`（`preprocess_episode` 路由）
- Modify: `server/agent_runtime/skill_function_declarations.py`（`_handle_preprocess_episode`）
- Test: `tests/test_projects_router.py`

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_projects_router.py` 的 `TestProjectsRouter` 內，`test_preprocess_episode_runtime_error_still_500` 之後新增：

```python
    def test_preprocess_episode_forwards_source(self, tmp_path, monkeypatch):
        """preprocess 路由把 body 的 source 傳給 run_preprocess。"""
        import lib.episode_preprocess as ep

        captured = {}

        def _fake(project_path, episode, *, content_mode=None, repo_root=None, source=None):
            captured["source"] = source
            return {"step1_path": "drafts/episode_2/step1_segments.md", "content_mode": "narration"}

        monkeypatch.setattr(ep, "run_preprocess", _fake)
        client = _client(monkeypatch, _FakePM(tmp_path), _FakeCalc())
        with client:
            r = client.post(
                "/api/v1/projects/ready/episodes/2/preprocess",
                json={"source": "source/我指定的.txt"},
            )
            assert r.status_code == 200
            assert captured["source"] == "source/我指定的.txt"

    def test_preprocess_episode_without_source_passes_none(self, tmp_path, monkeypatch):
        """preprocess 路由未帶 source 時，傳 None（走自動均分）。"""
        import lib.episode_preprocess as ep

        captured = {}

        def _fake(project_path, episode, *, content_mode=None, repo_root=None, source=None):
            captured["source"] = source
            return {"step1_path": "", "content_mode": "narration"}

        monkeypatch.setattr(ep, "run_preprocess", _fake)
        client = _client(monkeypatch, _FakePM(tmp_path), _FakeCalc())
        with client:
            r = client.post("/api/v1/projects/ready/episodes/2/preprocess")
            assert r.status_code == 200
            assert captured["source"] is None
```

- [ ] **Step 2: 跑測試，確認失敗**

Run: `uv run python -m pytest tests/test_projects_router.py -q -k "forwards_source or without_source"`
Expected: `test_preprocess_episode_forwards_source` FAIL（路由目前不讀 body、不傳 `source`）。

- [ ] **Step 3: 路由加可選 `source` body**

在 `server/routers/projects.py`：

於 `SplitEpisodeRequest` 附近（其他 request model 區域）新增：
```python
class PreprocessEpisodeRequest(BaseModel):
    source: str | None = None
```

`preprocess_episode` 簽名與內文改為：
```python
@router.post("/projects/{name}/episodes/{episode}/preprocess")
async def preprocess_episode(
    name: str,
    episode: int,
    _user: CurrentUser,
    req: PreprocessEpisodeRequest = PreprocessEpisodeRequest(),
):
    """Step 1 預處理：根據 content_mode 呼叫對應 skill 腳本。

    req.source 可選：指定本集原文來源；省略則自動按集數均分整本原文。
    """
    from lib.episode_preprocess import SourceNotReadyError, run_preprocess

    try:
        manager = get_project_manager()
        if not manager.project_exists(name):
            raise HTTPException(status_code=404, detail=f"專案 '{name}' 不存在")

        project = await asyncio.to_thread(manager.load_project, name)
        project_path = manager.get_project_path(name)
        with project_change_source("webui"):
            return await asyncio.to_thread(
                run_preprocess,
                project_path,
                episode,
                content_mode=project.get("content_mode", "narration"),
                repo_root=PROJECT_ROOT,
                source=req.source,
            )

    except SourceNotReadyError as e:
        raise HTTPException(status_code=400, detail=str(e))
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))
    except (FileNotFoundError, RuntimeError) as e:
        raise HTTPException(status_code=500, detail=str(e))
    except HTTPException:
        raise
    except Exception as e:
        logger.exception("請求處理失敗")
        raise HTTPException(status_code=500, detail=str(e))
```

說明：`req` 給預設值 `PreprocessEpisodeRequest()`，使前端不帶 body 時仍可呼叫（`source=None`）。FastAPI 對「無 body」的可選 model：若測試顯示不帶 body 會 422，改用 `req: PreprocessEpisodeRequest | None = None` 並在內文 `source = req.source if req else None`。Step 4 跑測試驗證採哪種。

- [ ] **Step 4: 跑路由測試**

Run: `uv run python -m pytest tests/test_projects_router.py -q -k preprocess`
Expected: 全部 PASS（含本 session 既有的 400／500 測試 + 新 2 測試）。若 `without_source` 因 422 失敗，依 Step 3 說明改用 `PreprocessEpisodeRequest | None = None` 寫法後重跑。

- [ ] **Step 5: agent 工具加可選 `source`**

在 `server/agent_runtime/skill_function_declarations.py` 的 `_handle_preprocess_episode`：

宣告區（`PREPROCESS_EPISODE_DECL` 的 `parameters.properties`）新增 `source` 可選欄位：
```python
        "properties": {
            "episode": {"type": "integer", "description": "集數編號"},
            "source": {
                "type": "string",
                "description": "可選。指定本集小說原文的相對路徑；省略則自動按集數均分整本原文。",
            },
        },
```
（`required` 維持只有 `["episode"]`。）

handler 內把 `source` 傳下去：
```python
    episode = args.get("episode")
    if not isinstance(episode, int) or episode < 1:
        return {"ok": False, "error": "invalid_argument", "reason": "需要 episode(int>=1)"}
    source = args.get("source")
    if source is not None and not isinstance(source, str):
        return {"ok": False, "error": "invalid_argument", "reason": "source 必須是字串"}
    project_path = ctx.project_manager.get_project_path(ctx.project_name)
    try:
        result = await asyncio.to_thread(
            run_preprocess,
            project_path,
            episode,
            repo_root=ctx.project_manager.projects_root.parent,
            source=source,
        )
        return {"ok": True, **result}
```
（其餘 `except` 分支不變。）

- [ ] **Step 6: 跑相關測試**

Run: `uv run python -m pytest tests/test_episode_split_skill.py tests/test_skill_function_declarations.py -q`
Expected: 全部 PASS。

- [ ] **Step 7: lint**

Run: `uv run ruff check server/routers/projects.py server/agent_runtime/skill_function_declarations.py tests/test_projects_router.py`
Expected: `All checks passed!`

- [ ] **Step 8: Commit**

```bash
git add server/routers/projects.py server/agent_runtime/skill_function_declarations.py tests/test_projects_router.py
git commit -m "feat: 拆段 API 與 agent 工具支援可選指定原文來源"
```

---

## Task 6：前端 — `preprocessEpisode` 支援可選 `source`

**Files:**
- Modify: `frontend/src/api/episodes.ts`（`preprocessEpisode`）

說明：本 Task 只讓前端 API client 能傳 `source`；UI 入口在 Task 7。

- [ ] **Step 1: 改 `preprocessEpisode` 簽名**

`frontend/src/api/episodes.ts` 的 `preprocessEpisode` 改為：
```typescript
  async preprocessEpisode(
    name: string,
    episode: number,
    source?: string,
  ): Promise<{ step1_path: string; content_mode: string }> {
    return getApi().request(
      `/projects/${encodeURIComponent(name)}/episodes/${episode}/preprocess`,
      {
        method: "POST",
        body: JSON.stringify(source ? { source } : {}),
      },
    );
  },
```

- [ ] **Step 2: typecheck**

Run: `cd frontend && pnpm build`
Expected: 構建成功，無型別錯誤。

- [ ] **Step 3: Commit**

```bash
git add frontend/src/api/episodes.ts
git commit -m "feat: 前端 preprocessEpisode 支援可選指定原文來源"
```

---

## Task 7：前端 — 拆段按鈕加「指定來源」入口

**Files:**
- Modify: `frontend/src/components/canvas/timeline/EpisodeActionsBar.tsx`

說明：拆段按鈕點擊時，預設直接走自動均分（不指定來源）。新增「指定來源」的入口——本計畫採**最小侵入**做法：拆段按鈕旁加一個小型「⋯」次選單／或一個獨立的小按鈕「指定原文拆段」，點擊後彈出讓使用者選 `source/` 內的檔案。`EpisodeActionsBar` 已能取得 `projectName`／`episode`；`source/` 檔案清單可用既有 API（`TimelineCanvas` 已有 `sourceFiles` state，見 `TimelineCanvas.tsx:99`）。

實作要點（執行者依現有元件風格調整；以下為行為規格）：
- 主「拆段／重新拆段」按鈕：行為不變，點擊→確認→`preprocessEpisode(projectName, episode)`（不帶 source，走均分）。
- 新增「指定原文」操作：彈出一個選單列出 `source/` 內檔案（排除 `_remaining.txt`），選定後呼叫 `preprocessEpisode(projectName, episode, 選定路徑)`。
- 確認文字：指定來源時用「將以《檔名》為原文重新拆段。確定？」。

- [ ] **Step 1: 取得 source 檔案清單**

確認 `source/` 檔案清單的取得方式。Run: `grep -rn "sourceFiles\|listSource\|source.*files" frontend/src/api frontend/src/components/canvas/timeline/TimelineCanvas.tsx`
依結果決定：若 `TimelineCanvas` 已把 `sourceFiles` 傳給 `EpisodeActionsBar` 則直接用；否則由 `EpisodeActionsBar` 自行呼叫既有 source 列表 API（參考 `files.ts` 內 source 相關 API）。將清單以 prop 或內部 state 取得。

- [ ] **Step 2: 加「指定原文」UI 與行為**

在 `EpisodeActionsBar.tsx` 拆段 `ActionButton` 旁，新增一個次要入口（小按鈕或下拉），點擊展開 `source/` 檔案清單；選定檔案後：
```typescript
const confirmed = await confirm({
  message: `將以《${fileName}》為原文重新拆段。確定？`,
});
if (confirmed) {
  // handlePreprocess 改為可接受 source 參數
  void handlePreprocessWithSource(`source/${fileName}`);
}
```
`handlePreprocess` 抽出共用核心 `handlePreprocessWithSource(source?: string)`，內部呼叫 `episodesApi.preprocessEpisode(projectName, episode, source)`；原無參數的主按鈕呼叫 `handlePreprocessWithSource(undefined)`。

- [ ] **Step 3: typecheck + 既有測試**

Run: `cd frontend && pnpm check`
Expected: typecheck 通過；`EpisodeActionsBar` 相關測試（若有）通過。若 `TimelineCanvas.test.tsx` 因新增 UI 而需調整 selector，一併修正。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/canvas/timeline/EpisodeActionsBar.tsx frontend/src/components/canvas/timeline/TimelineCanvas.tsx
git commit -m "feat: 拆段可選擇指定原文來源"
```

---

## Task 8：真實專案驗證 + 文件同步 + 全套迴歸

**Files:**
- 驗證對象：`projects/project-76741dd3`
- 檢查（必要時 Modify）：`agent_runtime_profile/.claude/skills/generate-script/SKILL.md`

- [ ] **Step 1: 對真實專案驗證第 2 集可拆（自動均分）**

Run:
```bash
cd projects/project-76741dd3 && uv run --project /home/human/arcreel360 python /home/human/arcreel360/agent_runtime_profile/.claude/skills/generate-script/scripts/split_narration_segments.py --episode 2 --dry-run 2>&1 | head -5; echo "rc=${PIPESTATUS[0]}"
```
Expected: `rc=0`，輸出 `DRY RUN` Prompt 開頭。

- [ ] **Step 2: 驗證第 1 集正常 + 指定來源正常**

Run:
```bash
cd projects/project-76741dd3 && uv run --project /home/human/arcreel360 python /home/human/arcreel360/agent_runtime_profile/.claude/skills/generate-script/scripts/split_narration_segments.py --episode 1 --dry-run 2>&1 | head -3; echo "rc1=${PIPESTATUS[0]}"
uv run --project /home/human/arcreel360 python /home/human/arcreel360/agent_runtime_profile/.claude/skills/generate-script/scripts/split_narration_segments.py --episode 2 --source "source/我的刀盾_第1章.md" --dry-run 2>&1 | head -3; echo "rc2=${PIPESTATUS[0]}"
```
Expected: `rc1=0`、`rc2=0`（指定來源也能跑）。

- [ ] **Step 3: 檢查 SKILL.md 是否需同步**

Run: `grep -n "episode_.*txt\|分集\|原文\|--source" agent_runtime_profile/.claude/skills/generate-script/SKILL.md`
若有描述原文來源解析且與新行為不符，更新為：
```
原文來源：
- 指定 --source 時讀該檔。
- 未指定時，優先讀 source/episode_{N}.txt；若無，讀 source/ 內所有原文檔
  （按檔名串接），依 project.json 集數均分後取本集片段。
```
若無命中則無需改動。

- [ ] **Step 4: 全套迴歸**

Run: `uv run python -m pytest tests/ -q`
Expected: 全部 PASS，無 FAIL。

Run: `cd frontend && pnpm check`
Expected: typecheck + 前端測試通過。

- [ ] **Step 5: Commit（僅當 Step 3 有實際改動）**

```bash
git add agent_runtime_profile/.claude/skills/generate-script/SKILL.md
git commit -m "docs: 同步拆段原文來源解析說明"
```

---

## Self-Review

**1. Spec coverage（對照使用者意圖）**
- 「沒指定就自動按集數分」→ Task 1 `split_into_n_episodes` + Task 2/3 `resolve_novel_text` 第 3 條規則。✅
- 「有指定就用指定的」→ `resolve_novel_text` 第 1 條規則（已存在，新增覆寫測試）；傳遞鏈 Task 4（run_preprocess）→ Task 5（路由／agent）→ Task 6（前端 API）→ Task 7（前端 UI）。✅
- 「整本 = source/ 所有檔按檔名串起來」→ `_read_whole_source` 按檔名 sorted 串接。✅
- 「共幾集 = project.json 現有集數」→ `_episode_index_and_total`。✅
- 不需「分集面板」即可拆任何集 → 均分即時計算。✅
- narration + drama 一致 → Task 2／Task 3 對稱。✅
- 真實專案 project-76741dd3 第 2 集可拆 → Task 8 驗證。✅

**2. Placeholder scan**：純函式與後端 Task（1-6）均含完整測試碼與實作碼。Task 7（前端 UI）以「行為規格 + 程式片段」描述——因前端 UI 細節需依現有元件風格（下拉／選單樣式）調整，故給規格而非逐字 UI 程式碼；關鍵呼叫（`preprocessEpisode`、`confirm`、`handlePreprocessWithSource`）皆具體列出。此為前端 UI Task 的合理粒度，非佔位符。✅

**3. Type consistency**：
- `split_into_n_episodes(text: str, total_episodes: int) -> list[str]`：Task 1 定義，Task 2/3 呼叫一致。
- `resolve_novel_text(project_path, episode, source)`：簽名不變。
- `_episode_index_and_total -> tuple[int, int]`：回傳 `(index, total)`，`parts[index - 1]` 一致。
- `_read_whole_source(source_dir) -> str`：Task 2/3 同名同簽名。
- `run_preprocess(..., source: str | None = None)`：Task 4 定義，Task 5 路由與 agent handler 以 `source=` 傳入一致。
- `preprocessEpisode(name, episode, source?)`：Task 6 定義，Task 7 呼叫一致。
- `PreprocessEpisodeRequest`：Task 5 新增，僅該 Task 內使用。
- `handlePreprocessWithSource(source?: string)`：Task 7 內定義與呼叫一致。✅

### 已知取捨（執行者須知）

- **動態分母**：集數變動使已拆集的均分起迄位移。使用者已確認接受。
- **均分以「有效字數」為基準**（`count_chars`，不含空行），切點挪最近句末標點——段落非嚴格等長（語義完整優先）。
- **`project.json` 缺失／集不在列表**：`_episode_index_and_total` 退化為 `(1,1)`，整本當單一集。
- **既有分集切分機制（`_remaining.txt`、split 路由）保留不動**：其產物 `episode_N.txt` 優先序高於均分。
- **FastAPI 可選 body**：Task 5 Step 3 已標註若 `PreprocessEpisodeRequest = PreprocessEpisodeRequest()` 預設值導致不帶 body 時 422，改用 `PreprocessEpisodeRequest | None = None`；Step 4 測試會明確驗出採哪種。
```
