# 時間線原文雙向同步與提示詞佈局改造 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 實現劇集工作區分鏡/影片原文與 Step 1 預處理草稿的雙向即時同步，並將分鏡/影片的「提示詞選模型 + AI 生成」控制項移到輸入框下方以最佳化佈局。

**Architecture:**
1. 後端新增 `lib/step1_draft_sync.py` 專職解析/渲染草稿與執行雙向同步邏輯。
2. 整合後端路由，在更新 segment 內容、場景描述以及儲存草稿時，主動觸發同步。
3. 前端 `PromptColumn` 將選模型及 AI 生成控制項改為垂直佈局放至底部。
4. 前端 `TextColumn` 補上 drama 模式場景描述的編輯框，並共用 mention 編輯邏輯。

**Tech Stack:** Python, FastAPI, React 19, Tailwind CSS 4, pytest, ruff.

---

### Task 1: 新增後端共用同步服務

**Files:**
- Create: `lib/step1_draft_sync.py`
- Test: `tests/test_step1_draft_sync.py`

- [x] **Step 1: 寫失敗測試**

```python
# tests/test_step1_draft_sync.py
import pytest
from lib.step1_draft_sync import parse_draft_table, render_draft_table

def test_parse_and_render_roundtrip():
    md_content = (
        "| 片段 ID | 原文 | 字數 | 時長 | segment_break |\n"
        "|---|---|---|---|---|\n"
        "| G01 | 測試內容第一行 | 7 | 4 | True |\n"
        "| G02 | 測試內容第二行 | 7 | 4 | False |\n"
    )
    rows = parse_draft_table(md_content)
    assert len(rows) == 2
    assert rows[0]["id"] == "G01"
    assert rows[0]["content"] == "測試內容第一行"

    rendered = render_draft_table(rows, "narration")
    assert "測試內容第一行" in rendered
    assert "| 7 |" in rendered  # 長度應正確重新計算
```

- [x] **Step 2: 執行確認失敗**

Run: `uv run python -m pytest tests/test_step1_draft_sync.py -v`
Expected: FAIL (ModuleNotFoundError)

- [x] **Step 3: 實作 `lib/step1_draft_sync.py`**

```python
import re
import logging
from dataclasses import dataclass
from pathlib import Path

logger = logging.getLogger(__name__)

@dataclass(frozen=True)
class DraftMode:
    filename: str
    id_header_aliases: set[str]
    segment_field: str
    script_items_key: str
    item_id_key: str

DRAFT_MODES = {
    "narration": DraftMode(
        filename="step1_segments.md",
        id_header_aliases={"片段", "片段 ID", "片段ID", "ID"},
        segment_field="novel_text",
        script_items_key="segments",
        item_id_key="segment_id",
    ),
    "drama": DraftMode(
        filename="step1_normalized_script.md",
        id_header_aliases={"場景 ID", "場景ID", "ID"},
        segment_field="scene_description",
        script_items_key="scenes",
        item_id_key="scene_id",
    ),
}

def parse_draft_table(md: str) -> list[dict]:
    rows = []
    lines = md.splitlines()
    for line in lines:
        line = line.strip()
        if not line.startswith("|") or "---" in line:
            continue
        parts = [p.strip() for p in line.split("|")]
        # parts[0] is empty, parts[1] is ID, parts[2] is content, subsequent are extra cells
        if len(parts) >= 3:
            id_val = parts[1]
            if id_val in {"片段", "場景 ID", "片段 ID", "場景ID", "片段ID", "ID"}:
                continue
            rows.append({
                "id": id_val,
                "content": parts[2],
                "raw_cells": parts[3:-1]  # markdown ends with '|', split leaves an empty string at the end
            })
    return rows

def render_draft_table(rows: list[dict], mode_name: str) -> str:
    is_drama = mode_name == "drama"
    id_header = "場景 ID" if is_drama else "片段 ID"
    content_header = "場景描述" if is_drama else "原文"
    
    headers = [id_header, content_header, "字數", "時長", "segment_break"]
    lines = [
        "| " + " | ".join(headers) + " |",
        "| " + " | ".join(["---"] * len(headers)) + " |"
    ]
    for row in rows:
        content = row["content"]
        char_count = str(len(content))
        # 沿用原有的額外欄位，或者預設值
        raw = row["raw_cells"]
        duration = raw[1] if len(raw) > 1 else "4"
        seg_break = raw[2] if len(raw) > 2 else "False"
        
        cells = [row["id"], content, char_count, duration, seg_break]
        lines.append("| " + " | ".join(cells) + " |")
    return "\n".join(lines) + "\n"

def sync_segment_to_draft(project_path: Path, episode: int, segment_id: str, new_content: str, mode_name: str) -> None:
    mode = DRAFT_MODES[mode_name]
    draft_dir = project_path / "drafts" / f"episode_{episode}"
    draft_file = draft_dir / mode.filename
    if not draft_file.exists():
        return  # 不存在則略過

    try:
        md = draft_file.read_text(encoding="utf-8")
        rows = parse_draft_table(md)
        updated = False
        for row in rows:
            if row["id"] == segment_id:
                row["content"] = new_content
                updated = True
                break
        if updated:
            new_md = render_draft_table(rows, mode_name)
            draft_file.write_text(new_md, encoding="utf-8")
    except Exception as e:
        logger.error(f"Sync segment to draft failed: {e}", exc_info=True)

def sync_draft_to_segments(project_path: Path, episode: int, new_md: str, mode_name: str, manager) -> int:
    mode = DRAFT_MODES[mode_name]
    rows = parse_draft_table(new_md)
    script_filename = f"episode_{episode}.json"
    
    try:
        script = manager.load_script(project_path.name, script_filename)
    except FileNotFoundError:
        return 0

    items = script.get(mode.script_items_key, [])
    if not items:
        # 相容以 dict 呈現的 scenes 結構
        items = script.get("scenes", {})
        
    updated_count = 0
    
    # 建立 ID 對應 map
    content_map = {row["id"]: row["content"] for row in rows}
    
    if isinstance(items, list):
        for item in items:
            iid = item.get(mode.item_id_key)
            if iid in content_map:
                if item.get(mode.segment_field) != content_map[iid]:
                    item[mode.segment_field] = content_map[iid]
                    updated_count += 1
    elif isinstance(items, dict):
        for iid, item in items.items():
            if iid in content_map:
                if item.get(mode.segment_field) != content_map[iid]:
                    item[mode.segment_field] = content_map[iid]
                    updated_count += 1
                    
    if updated_count > 0:
        manager.save_script(project_path.name, script, script_filename)
        
    return updated_count
```

- [x] **Step 4: 執行確認通過**

Run: `uv run python -m pytest tests/test_step1_draft_sync.py -v`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add lib/step1_draft_sync.py tests/test_step1_draft_sync.py
```

---

### Task 2: 後端既有 API 路由改引用與同步擴散

**Files:**
- Modify: `server/routers/project_episodes.py`
- Modify: `server/routers/projects.py`
- Modify: `server/routers/files.py`

- [x] **Step 1: 寫失敗測試 (於 `tests/test_step1_draft_sync.py` 擴充整合測試)**

```python
# 測試 projects.py 與 files.py 的路由是否正確調用了 sync 方法
# 由於 FastAPI router 測試通常在 test_scene_routes.py 等處，
# 我們可以針對 get_source_paragraphs 新增一個單元測試驗證 parse 抽離後的正確性
```

- [x] **Step 2: 修改 `server/routers/project_episodes.py`**

- 匯入：`from lib.step1_draft_sync import parse_draft_table`
- 修改 `get_source_paragraphs` 中的 `_sync` 內部解析邏輯：
```python
# 替換第 785-804 行的 parsing 區塊：
            # 解析 Markdown 表格
            with open(draft_file, encoding="utf-8") as f:
                content = f.read()
            
            rows = parse_draft_table(content)
            paragraphs = [row["content"] for row in rows]
            return {"paragraphs": paragraphs}
```

- [x] **Step 3: 修改 `server/routers/projects.py`**

- 匯入：
  ```python
  import re
  from lib.step1_draft_sync import sync_segment_to_draft
  ```
- 輔助方法：
  ```python
  def extract_episode_num(script_file: str) -> int:
      match = re.search(r"episode_(\d+)", script_file)
      if not match:
          raise ValueError(f"無法從檔案名 {script_file} 解析出劇集編號")
      return int(match.group(1))
  ```
- 修改 `update_segment` 路由：
  ```python
  # 在第 785 行 save_script 之後呼叫：
              with project_change_source("webui"):
                  manager.save_script(name, script, req.script_file)
                  try:
                      ep = extract_episode_num(req.script_file)
                      project_path = manager.get_project_path(name)
                      if "novel_text" in req.model_fields_set and req.novel_text is not None:
                          sync_segment_to_draft(project_path, ep, segment_id, req.novel_text, "narration")
                  except Exception as e:
                      logger.error(f"Sync segment to draft failed in update_segment: {e}")
  ```
- 修改 `update_scene` 路由：
  * 在白名單 `key in [...]` 中加入 `"scene_description"`。
  * 在第 607 行 `save_script` 之後呼叫：
  ```python
              with project_change_source("webui"):
                  manager.save_script(name, script, req.script_file)
                  try:
                      ep = extract_episode_num(req.script_file)
                      project_path = manager.get_project_path(name)
                      if "scene_description" in req.updates:
                          new_desc = req.updates["scene_description"]
                          if new_desc is not None:
                              sync_segment_to_draft(project_path, ep, scene_id, new_desc, "drama")
                  except Exception as e:
                      logger.error(f"Sync scene to draft failed in update_scene: {e}")
  ```

- [x] **Step 4: 修改 `server/routers/files.py`**

- 匯入：
  ```python
  from lib.step1_draft_sync import sync_draft_to_segments
  ```
- 修改 `update_draft_content` 路由：
  ```python
  # 在第 558 行 draft_path.write_text(content, encoding="utf-8") 之後呼叫：
              if step_num == 1:
                  try:
                      sync_draft_to_segments(project_dir, episode, content, content_mode, get_project_manager())
                  except Exception as e:
                      logger.error(f"Sync draft to segments failed in update_draft_content: {e}")
  ```

- [x] **Step 5: 驗證測試與 Commit**

- 執行 `uv run python -m pytest tests/ -v`
- `git add server/routers/`

---

### Task 3: 後端同步單元測試與 Ruff 檢查

**Files:**
- Create/Modify: `tests/test_step1_draft_sync.py`

- [x] **Step 1: 寫完整的 mock 整合測試**

測試修改 segment 是否正確寫入草稿，以及草稿 PUT 時是否正確同步到 json script。

- [x] **Step 2: 執行 pytest 測試**

Run: `uv run python -m pytest tests/test_step1_draft_sync.py -v`
Expected: PASS

- [x] **Step 3: 執行 ruff 檢查**

Run: `uv run ruff check lib/step1_draft_sync.py server/routers/projects.py && uv run ruff format --check lib/step1_draft_sync.py`
Expected: PASS

---

### Task 4: 前端提示詞佈局改造 (`PromptColumn`)

**Files:**
- Modify: `frontend/src/components/canvas/timeline/segment-card/PromptColumn.tsx`

- [x] **Step 1: 新增 `PromptModelToolbar` 元件**

在 `PromptColumn.tsx` 中，我們直接在 `PromptColumn.tsx` 頂部定義（或單獨定義）：

```tsx
interface PromptModelToolbarProps {
  textModel: string | null;
  setTextModel: (model: string | null) => void;
  textModelOptions?: string[];
  providerNames?: Record<string, string>;
  onGenerate: () => void;
  isGenerating: boolean;
  btnTitle: string;
}

export function PromptModelToolbar({
  textModel,
  setTextModel,
  textModelOptions,
  providerNames,
  onGenerate,
  isGenerating,
  btnTitle,
}: PromptModelToolbarProps) {
  if (!textModelOptions || textModelOptions.length === 0) return null;

  return (
    <div className="flex items-center gap-2 mt-1.5 w-full">
      <ProviderModelSelect
        value={textModel || ""}
        options={textModelOptions}
        providerNames={providerNames || {}}
        onChange={setTextModel}
        placeholder="選模型..."
        allowDefault={true}
        defaultLabel="預設文字模型"
        className="flex-1 text-xs"
        size="sm"
      />
      <button
        type="button"
        onClick={onGenerate}
        disabled={isGenerating}
        className="flex items-center justify-center gap-1 px-3 py-1.5 text-xs font-semibold text-indigo-400 hover:text-indigo-300 disabled:opacity-50 disabled:cursor-not-allowed focus-visible:outline-none border border-gray-700/50 hover:border-gray-600/50 bg-gray-800/40 rounded-md hover:bg-gray-800/80 transition-all shrink-0"
        title={btnTitle}
      >
        <Sparkles className={`h-3 w-3 ${isGenerating ? "animate-spin" : ""}`} />
        {isGenerating ? "生成中..." : "AI 產生"}
      </button>
    </div>
  );
}
```

- [x] **Step 2: 改造 Image Prompt / Video Prompt 區塊**

- 移除標題列右側原有的 `ProviderModelSelect` 及按鈕。
- 在 textarea / editor 下方加入 `PromptModelToolbar`。

- [x] **Step 3: 驗證編譯是否正常**

Run: `pnpm check`
Expected: PASS

---

### Task 5: 前端 Drama 模式場景描述可編輯與同步

**Files:**
- Modify: `frontend/src/components/canvas/timeline/segment-card/helpers.ts`
- Modify: `frontend/src/components/canvas/timeline/segment-card/TextColumn.tsx`

- [x] **Step 1: 修改 `helpers.ts`**

修改 `getSourceMentionValue` 支持 `scene_description`：
```typescript
export function getSourceMentionValue(segment: Segment, mode: "narration" | "drama"): string {
  return mode === "narration"
    ? ((segment as NarrationSegment).novel_text ?? "")
    : ((segment as DramaScene).scene_description ?? "");
}
```

- [x] **Step 2: 修改 `TextColumn.tsx`**

- 抽取共用的 `sourceEditor`。
- 修改 drama 模式下的渲染，在對話列表下方新增場景描述的編輯框，並綁定為與 narration 相同的 state 和更新回調。

- [x] **Step 3: 驗證編譯**

Run: `pnpm check`
Expected: PASS
