## 1. 主题基础收敛

- [ ] 1.1 审计 `frontend/src/index.css`、`frontend/src/css/app.css`、`frontend/src/css/styles.css` 中现有颜色、字体、边框、阴影定义，标记冲突和重复来源
- [ ] 1.2 在主题入口建立语义化 token，覆盖背景、panel、浮层、主文字、次文字、主强调色、次强调色、成功、警告、危险等角色
- [ ] 1.3 将高频交互态（hover、focus、active、disabled）整理为统一规则，避免继续在组件中零散写死 `indigo-*` 和孤立色值
- [ ] 1.4 为全局 surface / panel / status 形成可复用样式约定，并保留过渡兼容层，避免一次性打断现有页面

## 2. 工作台壳层刷新

- [ ] 2.1 重做项目大厅页面背景、顶部操作区与项目卡片层级，使入口页符合创作型工作台的视觉定位
- [ ] 2.2 调整项目卡片的封面区、标题区、进度区和辅助元信息区层级，提升首屏扫描效率
- [ ] 2.3 刷新 `GlobalHeader` 的当前项目、阶段、状态与操作按钮层级，减少头部噪音
- [ ] 2.4 刷新 `StudioLayout` 的三栏表面、边界与展开态，明确资产侧栏、主编辑区与助手面板的职责差异

## 3. 设置中心重构

- [ ] 3.1 将系统配置页壳层升级为工作台内设置中心，明确头部摘要区、导航区、警示区和内容区
- [ ] 3.2 调整设置页导航与内容容器的表面层级，避免侧栏、主区和警示横幅全部停留在同层深灰
- [ ] 3.3 重构 `AgentConfigTab` 等高密度表单区的字段标签、说明、当前值和危险操作层级，改善阅读节奏
- [ ] 3.4 将设置页内警告文案与视觉语义更新为 provider-aware 工作台状态表达，并确保与当前 assistant provider 一致

## 4. 组件回收与样式去硬编码

- [ ] 4.1 清理壳层与设置页相关组件中的高频 `gray-*` / `indigo-*` 硬编码，替换为新的语义 token 或统一样式约定
- [ ] 4.2 对 `ProviderSection`、项目卡片、按钮、提示块等高频复用片段做样式收敛，避免新旧主题并存
- [ ] 4.3 回收或降级与新主题冲突的遗留 neon 风格样式，确保最终视觉语言不再分裂

## 5. 验证与收尾

- [ ] 5.1 为设置页与关键壳层页面补充或更新前端测试断言，覆盖新文案、层级入口与关键视觉状态
- [ ] 5.2 执行前端检查与目标页面手动验收，确认项目大厅、工作区壳层、设置页在桌面和移动宽度下都能正常显示
- [ ] 5.3 清点仍未纳入新视觉体系的页面或组件，记录为后续扩展范围，避免本次 change 范围失控