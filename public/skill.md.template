# ArcReel Skill

ArcReel 是一個 AI 影片生成平臺，將小說轉化為短影片。透過此 Skill，你可以使用自然語言對話來呼叫 ArcReel 的專案管理和內容創作能力。

## 認證

所有 API 請求需要在 Header 中傳遞 API Key：

```
Authorization: Bearer arc-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

API Key 可從 ArcReel 設定頁面的「API Keys」管理區取得。

## 工作流

推薦的完整創作工作流：

1. **建立專案**：`POST {{BASE_URL}}/api/v1/projects`
2. **上傳小說內容並生成概述**：`POST {{BASE_URL}}/api/v1/projects/{name}/source`
3. **多輪 Agent 對話**（創作劇本、分鏡等）：`POST {{BASE_URL}}/api/v1/agent/chat`
4. **查看生成結果**：`GET {{BASE_URL}}/api/v1/projects/{name}`

## 工具定義

### 1. 專案列表

列出所有現有專案。

- **端點**：`GET {{BASE_URL}}/api/v1/projects`
- **認證**：需要 Bearer Token

```bash
curl {{BASE_URL}}/api/v1/projects \
  -H "Authorization: Bearer <API_KEY>"
```

- **回應範例**：
```json
{
  "projects": [
    {
      "name": "my-novel",
      "title": "我的小說",
      "scenes_count": 12,
      "status": "ready"
    }
  ]
}
```

### 2. 建立專案

建立新的影片創作專案。

- **端點**：`POST {{BASE_URL}}/api/v1/projects`
- **認證**：需要 Bearer Token

```bash
curl -X POST {{BASE_URL}}/api/v1/projects \
  -H "Authorization: Bearer <API_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"name": "project-id", "title": "專案標題"}'
```

- **回應**：201，返回專案詳情

### 3. 上傳小說內容並生成概述（新專案必須）

將小說內容傳入專案，並自動觸發 AI 分析生成概述。支援兩種方式：**直接提交文字** 或 **上傳檔案**。

- **端點**：`POST {{BASE_URL}}/api/v1/projects/{name}/source`
- **認證**：需要 Bearer Token
- **Content-Type**：`multipart/form-data`
- **表單欄位**：

| 欄位 | 類型 | 說明 |
|------|------|------|
| `content` | string（文字欄位） | 小說文字內容，與 `file` 二選一，最大 200000 字元 |
| `file` | file（檔案欄位） | 上傳 .txt/.md 檔案，檔名自動取自原始檔名，與 `content` 二選一 |
| `generate_overview` | bool | 是否自動生成概述，預設 `true` |

**方式 A：提交文字內容**（檔案自動命名為 `novel.txt`）

```bash
curl -X POST {{BASE_URL}}/api/v1/projects/my-novel/source \
  -H "Authorization: Bearer <API_KEY>" \
  -F "content=第一章\n\n這是小說的正文內容……" \
  -F "generate_overview=true"
```

**方式 B：上傳檔案**（檔名取自上傳檔案）

```bash
curl -X POST {{BASE_URL}}/api/v1/projects/my-novel/source \
  -H "Authorization: Bearer <API_KEY>" \
  -F "file=@my-novel.txt" \
  -F "generate_overview=true"
```

- **回應範例**：
```json
{
  "success": true,
  "filename": "my-novel.txt",
  "chars": 12345,
  "overview": {
    "synopsis": "一個關於……的故事",
    "genre": "懸疑",
    "theme": "人性與救贖",
    "world_setting": "現代都市"
  }
}
```
  - 若 `generate_overview=false`，回應中無 `overview` 欄位
  - 若概述生成失敗，回應包含 `overview: null` 和 `overview_error` 說明，檔案仍寫入成功

### 4. 取得專案詳情

取得專案的完整資訊，包括已生成的劇本和分鏡狀態。

- **端點**：`GET {{BASE_URL}}/api/v1/projects/{name}`
- **認證**：需要 Bearer Token

```bash
curl {{BASE_URL}}/api/v1/projects/my-novel \
  -H "Authorization: Bearer <API_KEY>"
```

- **回應範例**：
```json
{
  "project": {
    "name": "my-novel",
    "title": "我的小說",
    "synopsis": "...",
    "status": "ready"
  }
}
```

### 5. 更新專案設定

修改專案配置（標題、風格等）。

- **端點**：`PATCH {{BASE_URL}}/api/v1/projects/{name}`
- **認證**：需要 Bearer Token

```bash
curl -X PATCH {{BASE_URL}}/api/v1/projects/my-novel \
  -H "Authorization: Bearer <API_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"title": "新標題"}'
```

### 6. Agent 對話（劇本與創作）

向 ArcReel AI Agent 發送訊息，Agent 可執行劇本生成、分鏡創作、影片生成等複雜任務。

- **端點**：`POST {{BASE_URL}}/api/v1/agent/chat`
- **認證**：需要 Bearer Token

```bash
curl -X POST {{BASE_URL}}/api/v1/agent/chat \
  -H "Authorization: Bearer <API_KEY>" \
  -H "Content-Type: application/json" \
  -d '{
    "project_name": "my-novel",
    "message": "幫我為第一章生成劇本，風格要偏向懸疑",
    "session_id": null
  }'
```

  - `project_name`：專案 ID（必填）
  - `message`：發送給 Agent 的訊息（必填）
  - `session_id`：會話 ID，傳入已有 ID 可繼續多輪對話，不傳則建立新會話

- **回應範例**：
```json
{
  "session_id": "sess-abc123",
  "reply": "好的，我已經為第一章生成了懸疑風格的劇本...",
  "status": "completed"
}
```
  - `status` 可能為：`completed`（成功）、`timeout`（逾時，含部分回應）、`error`（出錯）

## 使用範例

### 從小說文字到影片專案（推薦完整流程）

```bash
# 1. 建立專案
curl -X POST {{BASE_URL}}/api/v1/projects \
  -H "Authorization: Bearer <API_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"name": "ghost-story", "title": "鬼屋探險"}'

# 2A. 上傳小說文字，自動生成概述
curl -X POST {{BASE_URL}}/api/v1/projects/ghost-story/source \
  -H "Authorization: Bearer <API_KEY>" \
  -F "content=第一章\n\n探險者張明在山區發現了一座廢棄的老宅……（完整小說文字）" \
  -F "generate_overview=true"

# 2B. 或者上傳檔案
curl -X POST {{BASE_URL}}/api/v1/projects/ghost-story/source \
  -H "Authorization: Bearer <API_KEY>" \
  -F "file=@ghost-story.txt" \
  -F "generate_overview=true"

# 3. 開始 Agent 對話，生成第一集劇本
curl -X POST {{BASE_URL}}/api/v1/agent/chat \
  -H "Authorization: Bearer <API_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"project_name": "ghost-story", "message": "請為這個故事生成第一集的分鏡劇本，大約5個場景"}'

# 4. 查看生成結果
curl {{BASE_URL}}/api/v1/projects/ghost-story \
  -H "Authorization: Bearer <API_KEY>"
```

## 注意事項

- `source` 介面最大 200000 字元（約 10 萬中文字）；超長小說建議分章節多次傳入（後續呼叫會覆蓋同名檔案）
- Agent 對話最長等待 120 秒，複雜任務可能返回 `status: "timeout"` 和部分回應，此時可繼續傳入同一 `session_id` 查詢進度
- 所有 API 回應均為 JSON 格式
- 專案名稱（`name`）僅支援字母、數字和連字符，建議使用英文或拼音
